{
  "name": "yu-html-format",
  "version": "1.0.7",
  "description": "format html & text & so on",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "echo \"Error: this is a OnlyJavascript package, it has no dev script ,you can find 'index.html' as the demo page\" && exit 1",
    "build": "babel src --out-dir lib"
  },
  "keywords": [
    "babel",
    "plugin-dev"
  ],
  "author": "watanabeyu",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.0",
    "babel-plugin-transform-runtime": "^6.23.0",
    "babel-preset-env": "^1.6.1",
    "babel-preset-stage-3": "^6.24.1"
  }
}
